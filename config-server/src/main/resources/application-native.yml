# 基础配置
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/properties/
server:
  port: 7072

#注册中心配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
